import { ItemMinPricePipe } from './item-min-price.pipe';

describe('ItemMinPricePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemMinPricePipe();
    expect(pipe).toBeTruthy();
  });
});
