import { ItemMinRatingPipe } from './item-min-rating.pipe';

describe('ItemMinRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemMinRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
