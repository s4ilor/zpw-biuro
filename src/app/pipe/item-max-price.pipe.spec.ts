import { ItemMaxPricePipe } from './item-max-price.pipe';

describe('ItemMaxPricePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemMaxPricePipe();
    expect(pipe).toBeTruthy();
  });
});
