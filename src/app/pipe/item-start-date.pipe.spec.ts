import { ItemStartDatePipe } from './item-start-date.pipe';

describe('ItemStartDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemStartDatePipe();
    expect(pipe).toBeTruthy();
  });
});
