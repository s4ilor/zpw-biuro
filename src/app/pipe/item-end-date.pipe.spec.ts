import { ItemEndDatePipe } from './item-end-date.pipe';

describe('ItemEndDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemEndDatePipe();
    expect(pipe).toBeTruthy();
  });
});
