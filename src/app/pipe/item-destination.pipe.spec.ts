import { ItemDestinationPipe } from './item-destination.pipe';

describe('ItemDestinationPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemDestinationPipe();
    expect(pipe).toBeTruthy();
  });
});
